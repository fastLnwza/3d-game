# วิธีติดตั้งและตั้งค่า CMake บน macOS

## ✅ สถานะปัจจุบัน

CMake ติดตั้งอยู่แล้วที่ `/opt/homebrew/bin/cmake` (เวอร์ชัน 4.1.0)

ปัญหาคือ: PATH ไม่ได้ include `/opt/homebrew/bin` ทำให้ Cursor/VSCode หา CMake ไม่เจอ

## 🔧 วิธีแก้ไข

### วิธีที่ 1: ตั้งค่าใน Cursor/VSCode (แนะนำ - แก้ไขทันที)

ไฟล์ `.vscode/settings.json` ถูกสร้างไว้แล้วในโปรเจค ซึ่งจะทำให้ Cursor ใช้ CMake path ที่ถูกต้อง

หากยังไม่ทำงาน ลอง:
1. ปิดและเปิด Cursor ใหม่
2. หรือรัน Command Palette (Cmd+Shift+P) → "CMake: Select a Kit" → เลือก CMake ที่ `/opt/homebrew/bin/cmake`

### วิธีที่ 2: เพิ่ม PATH ไปใน .zshrc (แก้ไขถาวร)

รันคำสั่งนี้ใน Terminal:

```bash
echo 'export PATH="/opt/homebrew/bin:$PATH"' >> ~/.zshrc
source ~/.zshrc
```

หรือแก้ไขไฟล์ `~/.zshrc` ด้วยตนเอง:

```bash
nano ~/.zshrc
```

เพิ่มบรรทัดนี้:

```bash
export PATH="/opt/homebrew/bin:$PATH"
```

บันทึก (Ctrl+O, Enter) และออก (Ctrl+X) แล้วรัน:

```bash
source ~/.zshrc
```

### วิธีที่ 3: ตรวจสอบว่าทำงานหรือไม่

```bash
# ตรวจสอบ PATH
echo $PATH

# ตรวจสอบ CMake
cmake --version

# หรือใช้ full path
/opt/homebrew/bin/cmake --version
```

---

## วิธีที่ 1: ใช้ Homebrew (ถ้ายังไม่มี CMake)

### ขั้นตอนที่ 1: ติดตั้ง Homebrew (ถ้ายังไม่มี)

เปิด Terminal และรันคำสั่งนี้:

```bash
/bin/bash -c "$(curl -fsSL https://raw.githubusercontent.com/Homebrew/install/HEAD/install.sh)"
```

### ขั้นตอนที่ 2: ติดตั้ง CMake

```bash
brew install cmake
```

### ขั้นตอนที่ 3: ตรวจสอบการติดตั้ง

```bash
cmake --version
```

ควรเห็นเวอร์ชันของ CMake แสดงขึ้นมา

---

## วิธีที่ 2: ดาวน์โหลดจากเว็บไซต์ CMake

1. ไปที่ https://cmake.org/download/
2. ดาวน์โหลด CMake installer สำหรับ macOS (.dmg file)
3. เปิดไฟล์ .dmg และลาก CMake ไปใส่ใน Applications
4. เปิด CMake GUI และเลือก "Tools" > "How to Install For Command Line Use"
5. ตามคำแนะนำในการเพิ่ม CMake ไปใน PATH

---

## วิธีที่ 3: ใช้ MacPorts (ถ้าใช้ MacPorts อยู่แล้ว)

```bash
sudo port install cmake
```

---

## หลังจากติดตั้งเสร็จ

หลังจากติดตั้ง CMake เสร็จแล้ว คุณสามารถ build โปรเจคได้ด้วยคำสั่ง:

```bash
cd "/Users/nanthikarn/Documents/GitHub/3d-game"
mkdir -p build
cd build
cmake ..
cmake --build .
```

หรือใช้ build script ที่มีอยู่:

```bash
./build.sh
```

---

## แก้ปัญหา PATH

ถ้า CMake ติดตั้งแล้วแต่ยังหาไม่เจอ อาจจะต้องเพิ่มไปใน PATH:

สำหรับ Homebrew:
```bash
echo 'export PATH="/opt/homebrew/bin:$PATH"' >> ~/.zshrc
source ~/.zshrc
```

สำหรับ Intel Mac:
```bash
echo 'export PATH="/usr/local/bin:$PATH"' >> ~/.zshrc
source ~/.zshrc
```

---

## สำหรับ VSCode/Cursor

หลังจากติดตั้ง CMake แล้ว:

1. เปิด Command Palette (Cmd+Shift+P)
2. พิมพ์ "CMake: Select a Kit"
3. เลือก CMake executable ที่ติดตั้งไว้

หรือตั้งค่าใน settings.json:
```json
{
    "cmake.cmakePath": "/opt/homebrew/bin/cmake"  // สำหรับ Apple Silicon
    // หรือ
    "cmake.cmakePath": "/usr/local/bin/cmake"     // สำหรับ Intel Mac
}
```

